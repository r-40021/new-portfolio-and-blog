{
  "scripts": {
    "build": "cp -r static/img assets/ && tinacms build && hugo --minify",
    "dev": "npx tinacms dev -c \"hugo server\""
  },
  "dependencies": {
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tinacms": "^3.1.1"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.0.5",
    "@types/node": "^20.10.4"
  }
}
