{
  "scripts": {
    "build": "tinacms build && hugo -b $CF_PAGES_URL",
    "dev": "cp -r static/img assets/ && npx tinacms dev -c \"hugo server\""
  },
  "dependencies": {
    "postcss": "^8.4.32",
    "postcss-cli": "^10.1.0",
    "tinacms": "^1.5.27"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.5.37",
    "@types/node": "^20.10.4"
  }
}
